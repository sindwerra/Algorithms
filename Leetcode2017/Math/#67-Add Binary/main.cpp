string addBinary(string a, string b) {
        string result = "";
        int counta = a.length() - 1;
        int countb = b.length() - 1;
        int carry = 0;

        while (counta >= 0 || countb >= 0) {
          if (counta < 0) {}
        }
    }
